jwt:
  secret:"youssfiSecret"
server:
  port: 8000
  error:
    whitelabel:
      enabled: false
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/java_spring_security_youssfi_course?createDatabaseIfNotExist=true
    password: papi
    username: papi
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect